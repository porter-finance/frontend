import styled from 'styled-components'

export const ErrorText = styled.div`
  color: ${({ theme }) => theme.text1};
  font-size: 15px;
  font-weight: bold;
  line-height: 1.4;
  margin: 0 0 0 10px;
  position: relative;
  text-align: left;
  top: 1px;
`
